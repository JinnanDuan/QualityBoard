1、筛选不要在上面，直接放在表头上 -- done
2、最上面的“详细执行历史”是否可以去掉 -- done
3、是多选框不是单选框 -- done
4、表格列宽做到可调整且默认紧凑一点 -- done
5、drawer中基本信息 外部链接 通过分割线 或者 分块区分地更明显一些 -- done
6、将截图和测试报告等放到外部链接部分 -- done


1、调整列宽直接按下并拖动表头单元格中间线调整，不要有灰色的那个小直角图标 -- done
2、列宽自动调整要同时考虑表头和内容的大小，表头不要换行显示否则很丑 -- done
3、用例名列比较特殊，列宽可以固定一定的宽度，如果列内容被遮挡，可以悬浮显示 -- done
4、整体表格可以稍微再宽一点 -- done

1、筛选改回到表格上方显示，不要放在表头上了 -- done

1、多选不生效 -- done

1、不要勾选即生效，希望有一个确认按钮，完成多选后统一生效 -- done

1、用例名筛选也改称多选，但可以支持模糊查找可选项 -- done

1、确认后面加一个重置按钮 -- done

1、确认或者重置后，sql执行时长有时较长，请实现：转圈圈等待，禁止其他操作 -- done



1、拖动改大小时有蓝色小线 -- done

1、大小问题 -- done

1、抽屉问题 -- done
- 字体大小问题 -- done
- 抽屉有个问题是，如果只看直接展示的表格，根本不知道有抽屉功能，有没有什么方案可以提示一下，这里有内容可以点击 -- done

1、所有列，如果列内容被遮挡，可以直接遮挡内容，不换行，如果要看完整内容，可以悬浮显示。跟用例名列保持一致 -- done


只有被遮挡时才悬停显示 -- done
点击超链接和悬浮的tooltip时，不要显示drawer -- done

当每个列内容被遮挡，鼠标hover时出现包含完整名字的tooltip，此时想要双击tooltip内容进行全选后复制。这时发现会触发drawer弹出 -- done，用其他方式替代


新增显示单独一列，专门用于drawer的触发，其他地方点击不触发drawer -- done


点击「查看详情」可查看执行详情；双击被遮挡的列内容可选中复制 ，这句话去掉 -- done


操作的表头改为 查看详情，原先查看详细的超链接，改超一个抽屉小图标的超链接 -- done


现在来调整下上面的筛选区，筛选区有两个问题 -- done
1、1、多选占用空间太长 如果某个列，比如用例名，被选上过多时，会把筛选区撑很大 -- done
2、模糊匹配某个字符，只能勾选一次。然后就会下拉选项列表恢复到没有模糊匹配前显示所有的选型。如果模糊匹配后，多选几个需要多次筛选 -- done


这里针对一个筛选项，其实把 输入框 和 显示框合二为一了。是否有必要区分开，直接展示的作为显示框，在下拉的列表菜单最上方显示输入框？ --  done，实际实现后不好看，回退了


1、列支持排序